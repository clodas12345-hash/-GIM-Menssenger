              <div className="space-y-1">
                <span className="text-[10px] text-gray-400 uppercase font-bold tracking-wider">A mensagem foi enviada com sucesso no WhatsApp?</span>
              </div>

              <div className="space-y-2 pt-1">
                <div className="grid grid-cols-1 gap-2">
                  <button
                    type="button"
                    onClick={() => {
                      const url = buildWhatsAppLink(pendingConfirmState.contact.phone, pendingConfirmState.messageText, settings.sendMode);
                      openWhatsAppLink(url);
                    }}
                    className="bg-[#15181E] hover:bg-[#1F2229] border border-[#2A2D35] text-gray-300 font-bold py-3 px-3 rounded-lg text-xs uppercase tracking-wider flex items-center justify-center space-x-1.5 transition-colors cursor-pointer"
                  >
                    <RefreshCw className="w-4 h-4 text-[#A88B4B]" />
                    <span>Reabrir WhatsApp</span>
                  </button>
                </div>
